{
  "name": "restaurant-pos-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    // placeholder: install dependencies when initialising project
  }
}